export type Context = {
  userId?: string;
  role?: string;
  userIp?: string;
  reqId: string;
  route: string;
};

export type SocketContext = {
  userId?: string;
  role?: string;
  userIp?: string;
  reqId: string;
  route: string;
};
